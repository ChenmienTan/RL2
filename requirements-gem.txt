# Optional GEM Environment Dependencies
# These dependencies are only needed when using GEM environments with RL2
# Install with: pip install -r requirements-gem.txt

# Note: Replace with actual GEM package name once published
# For now, install from source:
# git clone https://github.com/axonrl/gem.git
# cd gem && pip install -e .

# Placeholder for GEM package (uncomment when available)
# gem-rl

# GEM's additional dependencies that may be needed
numpy
gymnasium
asyncio-throttle